# 6회차 전환 및 연속 개발 계획

> 이 문서는 T14 이후의 **단일 작업 진입점**이다.
> 카드 하나를 완료하면 검증 결과를 기록하고 사용자에게 보고한 뒤, 사용자 확인 전에는 다음 카드를 시작하지 않는다.
> Supabase·Vercel·환경변수처럼 사용자의 접근 권한이 필요한 단계는 이 문서의 초보자용 지침을 먼저 따른다.

## 현재 상태

| 항목 | 상태 |
|---|---|
| 기준 브랜치 | `main` |
| 기준 커밋 | T19 완료 `c06b2d6 feat: T19 세션 처리 원자성과 복구 강화`; T20 변경은 이 카드의 단일 로컬 커밋 |
| T00~T13 | 기존 구현 완료. T12 부하 테스트 증적은 미완료 |
| T14 계획 고정 | 완료 |
| T16 6회차 개편 | 완료 |
| T18 성과측정 무결성 | 완료 — 12쌍 완전대응 산식·관리자 outcomes API·화면·CSV 구현 |
| T19 세션·완료 처리 안정화 | 완료 — 원자 세션 RPC·점수 재조정·복구·만료·Cron 전량 페이지네이션 |
| 다음 카드 | T21 — Supabase 마이그레이션 (백업·행 수 확인 후 사용자 승인) |
| 운영 일정 | 2026-08-17 시작 ~ 2026-09-25 종료, 월~금 KST |
| 목표 규모 | 6회차 × 12문항 = 72문항 |
| Supabase 연결 | 정상. 서버 전용 키로 REST 읽기 확인, 12개 대상 테이블 모두 0건 |
| 마지막 로컬 검증 | T20 정적 검증·관리자 대용량 조회 테스트·기존 검증기·타입검사·프로덕션 빌드 통과. Supabase는 T20에서 변경하지 않음 |

## 확정 운영·측정 기준

- 회차는 6개이며 참가자 API의 유효 범위는 `1..6`이다. 7·8회차는 존재하지 않는 회차로 처리한다.
- 회차당 문항은 12개, 전체 문항은 72개다.
- 시즌은 1~3회차가 시즌1, 4~6회차가 시즌2다.
- 1회차는 2026-08-17~08-21, 2회차는 08-24~08-28, 3회차는 08-31~09-04,
  4회차는 09-07~09-11, 5회차는 09-14~09-18, 6회차는 09-21~09-25다.
- 사전 측정 M01~M12와 사후 측정 M01P~M12P를 모두 유지한다.
- 12개 측정쌍은 참가자가 사전·사후 양쪽의 12문항을 모두 확정한 경우에만 1차 KPI에 포함한다.
- 시간초과는 응답 없음이 아니라 오답으로 확정하며, 완전대응 여부는 12쌍 모두의 확정 응답으로 판정한다.
- 측정쌍은 모두 `MC4`이고 사전·사후 난이도 등급도 쌍마다 동일하게 맞춘다.
- 최종 유형 분포는 `MC4 55 / OX 14 / ORDER 2 / SHORT 1`이다.
- 최종 난이도 분포는 `L1 26 / L2 35 / L3 11`이다.
- M01·M03·M04·M08·M11·M12의 사전 문항은 MC4로 변환한다.
- 사전학습 열람 효과는 인과효과가 아니라 열람자·미열람자 간 연관지표로 표시한다.

## 문항 이동 매핑

새 문항 코드는 회차 이동 후의 코드다. 괄호 안은 현재 원본 코드이며, 원본 코드는 이 표에서만 추적한다.

| 새 회차 | 새 코드 순서 | 현재 원본 코드 순서 |
|---|---|---|
| 1회차 | `1-01`~`1-12` | `1-01`~`1-12` |
| 2회차 | `2-01`~`2-12` | `2-01`~`2-12` |
| 3회차 | `3-01`~`3-12` | `3-01, 3-02, 3-03, 3-05, 3-07, 3-08, 4-04, 4-07, 4-09, 3-10, 3-12, 4-12` |
| 4회차 | `4-01`~`4-12` | `4-01, 4-02, 5-03, 5-05, 5-06, 5-10, 5-12, 6-03, 6-05, 6-06, 6-08, 6-10` |
| 5회차 | `5-01`~`5-12` | `7-01`~`7-12` |
| 6회차 | `6-01`~`6-12` | `8-01`~`8-12` |

### 측정 문항쌍

| 측정 코드 | 사전 문항 | 사후 문항 |
|---|---|---|
| M01 | `1-05` | `5-04` |
| M02 | `1-06` | `5-05` |
| M03 | `1-07` | `5-06` |
| M04 | `1-08` | `5-07` |
| M05 | `1-09` | `5-08` |
| M06 | `1-10` | `5-09` |
| M07 | `2-05` | `6-04` |
| M08 | `2-06` | `6-05` |
| M09 | `2-07` | `6-06` |
| M10 | `2-08` | `6-07` |
| M11 | `2-09` | `6-08` |
| M12 | `2-10` | `6-09` |

측정 문항은 문항 이동 뒤에도 `measure_code`를 유지한다. 참가자 응답에는 `measureCode`, `anchorCode`, `level`을 계속 포함하지 않는다.

## 순차 작업 카드

### T14 — 연속 작업 계획 고정 — 완료

- 이 문서를 생성하고 `tasks/tasks.md`, `CLAUDE.md`, `README.md`에서 연결한다.
- 6회차 운영 기준, 문항 이동, 측정쌍, 카드 순서, 사용자 개입 원칙을 고정한다.
- 구현 파일과 실DB를 변경하지 않는다.

완료 조건: 세 문서가 이 파일을 가리키고, 다음 카드가 T15로 명확하며, 문항 매핑과 KPI 기준이 기록되어 있다.

### T15 — 로컬 환경 및 실DB 읽기 점검 — 완료

사용자 설정이 필요한 카드다. `.env.local`을 만든 뒤 진행한다.

- Supabase 연결을 확인하고 `/api/health`를 호출한다.
- 아래 행 수를 읽기 전용으로 조사한다: `quiz_round`, `quiz_item`, `participant`, `quiz_session`, `quiz_session_item`, `prelearning_view`.
- 세션·응답·열람 행이 하나라도 있으면 자동 삭제·재시드를 하지 않고 사용자 판단을 기다린다.
- 활동 데이터가 없을 때만 T16 이후의 시드·뷰 교체를 준비한다.

#### 사용자가 할 일 — 초보자용

1. Supabase Dashboard에서 현재 프로젝트를 연다.
2. `Project Settings → Data API`에서 `Project URL`을 확인한다.
3. `Project Settings → API Keys`에서 서버 전용 `service_role` 키를 확인한다. `anon` 키를 사용하지 않는다.
4. 저장소 루트의 `.env.example`을 복사해 `.env.local`을 만든다.
5. `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SESSION_SECRET`, `CRON_SECRET`, `ADMIN_LOGIN_ID`, `ADMIN_INIT_PASSWORD`를 입력한다.
6. `SESSION_SECRET`와 `CRON_SECRET`은 PowerShell에서 각각 아래처럼 생성한다.

   ```powershell
   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
   ```

7. `.env.local`과 `service_role` 키는 채팅·스크린샷·GitHub에 올리지 않는다.
8. 저장 후 채팅에 `T15 환경변수 작성 완료`라고만 회신한다. 실제 비밀값은 보내지 않는다.

검증 결과:

- `.env.local`의 `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SESSION_SECRET`, `CRON_SECRET`, `ADMIN_LOGIN_ID`, `ADMIN_INIT_PASSWORD`, `TZ`가 모두 입력되어 있고, `.env.local`은 Git 추적에서 제외되어 있다.
- 서버 전용 키를 화면에 출력하지 않고 Supabase REST를 읽기 전용 호출했다. `org_category`, `org_unit`, `department`, `app_config`, `quiz_round`, `quiz_item`, `participant`, `quiz_session`, `quiz_session_item`, `prelearning_view`, `ranking_snapshot`, `admin_user`가 모두 HTTP 200 및 `Content-Range */0`으로 확인됐다.
- 로컬 Next.js 서버는 `.env.local`을 읽고 정상 기동했다. `/api/health`는 `app_config`가 초기화되어 0건인 상태에서 `.single()`이 실패하므로 500을 반환한다. 이는 Supabase 연결 실패가 아니라 T16 시드 전의 예상 상태다.
- 의도한 전체 초기화가 완료되어 활동 데이터가 없으므로 T16 진행 조건을 충족한다. 기존 8회차 SQL은 6회차 개편 전까지 실행하지 않는다.

완료 조건: 환경변수 입력·보안 제외·실DB 읽기 점검·활동 데이터 확인을 완료했다. 다음 카드 T16은 사용자가 시작을 확인한 뒤 착수한다.

### T16 — 6회차 문항·일정·명세 개편 — 완료

- `db/items.json`과 `db/04_seed_items.sql`을 72문항 기준으로 같은 내용이 되게 재생성한다.
- 6개 회차와 문항별 12행, 7·8회차 부재, 유형·난이도 분포를 자동검사한다.
- `db/01_schema.sql`, `lib/quiz.ts` 등 회차 범위 검사를 `1..6`으로 조정한다.
- 활성 명세·문안·테스트·결정 기록의 `8주`, `8회차`, `96문항` 전제를 6회차 기준으로 정합화한다.
- 과거 리뷰 문서는 이력을 보존하고, 6회차 개편으로 대체된 판단을 명시한다.
- `db/validate-items.mjs`를 추가하고 `test:items` 명령으로 문항 JSON·SQL·회차 시드의 정합성을 자동검사한다.
- Supabase에는 접근하지 않았으며, 초기화된 실DB 상태(대상 테이블 0건)를 그대로 유지한다.
- 검증 결과: 6×12·72문항, MC4 55/OX 14/ORDER 2/SHORT 1, L1 26/L2 35/L3 11, 측정 12쌍, 회차 범위 1..6 통과.
- `node node_modules\\typescript\\bin\\tsc --noEmit`, `node --test lib\\grading.test.ts`(29/29), `node db\\validate-items.mjs`, `node node_modules\\next\\dist\\bin\\next build` 통과.

완료 조건: 6×12, 유형·난이도 분포, 일정, API 범위 검사가 모두 통과한다.

### T17 — 사전학습 및 법령 검증 — 완료

- 최종 주제에 맞춘 사전학습 본문 6건을 작성한다.
- 현재 파서가 지원하는 제목·문단·목록만 사용하고 6개 카드 번호를 검증한다.
- 법령·수치 문항의 근거를 확인하고 `legal_ref`와 해설을 동기화한다.
- 퀴즈를 먼저 시작한 뒤 사전학습을 열람한 경우 성과의 사전학습 열람군으로 세지 않는다.
- `db/prelearning.json`과 `db/06_seed_prelearning.sql`을 추가하고, 회차당 6개 카드·6회차 본문을 자동검증한다.
- `2-04`, `2-08`, `4-09`, `6-03`, `6-07`, `6-11`을 2026-08-12 현행 공식 조문과 대조해 문항·선택지·해설·근거를 수정했다.
- `v_prelearning_effect`를 `prelearning_view.viewed_at <= quiz_session.started_at` 조건으로 교체해 퀴즈 시작 후 재열람을 열람 효과군에서 제외한다.
- 검증 기록은 `spec/legal-sources.md`에 남겼고, `H4` 회귀 시나리오와 `test:prelearning` 검증 명령을 추가했다.
- Supabase에는 접근하지 않았으며, 초기화된 실DB 상태(대상 테이블 0건)를 그대로 유지한다.
- 검증 결과: 본문 6건·카드 36개, 문항은행 6×12·72문항·12쌍, 타입검사, 기존 29개 테스트, 프로덕션 빌드 통과.

완료 조건: 6개 본문이 비어 있지 않고, 문항·본문·근거·열람 시점 검사가 통과한다.

### T18 — 성과측정 무결성 — 완료

- `v_matched_pre_post`를 12쌍 완전대응 기준으로 교체한다.
- 대응 표본 수, 사전·사후 평균, 평균·중앙 향상폭, 향상자 비율을 산출한다.
- M01~M12 문항쌍별, A1·A2 앵커별, T01~T04 전이별 보조지표를 제공한다.
- 관리자 API `GET /api/admin/stats/outcomes`를 추가하고, 화면·CSV의 설명을 같은 산식에 맞춘다.
- `db/validate-outcomes.mjs`로 11쌍 제외, 12쌍 포함, 시간초과 오답, API·뷰 계약을 자동검사한다.
- 관리자 화면에 대응 표본 요약, M01~M12 쌍별 표, 앵커·전이 보조지표를 추가하고 `outcomes.csv`를 제공한다.
- 검증 결과: 성과측정 검사·문항은행 검사·사전학습 검사·기존 29개 테스트·타입검사·프로덕션 빌드 통과.

완료 조건: 11쌍만 확정한 참가자는 제외되고, 12쌍을 확정한 참가자는 포함되며, 시간초과가 오답으로 계산된다.

사용자 작업: 없음. 이 카드에서는 로컬 파일만 변경하며 Supabase에 SQL을 실행하지 않는다.

### T19 — 세션·완료 처리 안정화

- 세션과 12개 `quiz_session_item` 생성의 원자성을 확보한다.
- 동시 세션 생성, 마지막 답변 확정 실패, 점수 동시 갱신, 대량 만료, 결과·복습 교착을 해결한다.
- `db/07_session_functions.sql`에 `fn_create_quiz_session`, `fn_reconcile_quiz_session`, `fn_expire_quiz_session`을 추가하고 서버 RPC 호출로 연결한다.
- 완료·만료 세션의 결과 조회도 확정 문항 기준으로 score를 재조정하며, 만료 Cron은 1,000행 제한을 넘겨 전량 처리한다.
- DB 함수 SQL은 로컬 파일로만 작성했다. Supabase 적용은 T21의 백업·행 수 확인과 별도 명시 승인을 받은 뒤 수행한다.
- 정적 세션 검증기와 L1~L8 회귀 시나리오를 추가한다.

완료 조건: 경합·재시도·마지막 문항 실패·만료 경계 검증이 통과한다.

검증 결과: `validate-session-functions.mjs`, 문항은행·사전학습·성과검사, 기존 29개 테스트, 타입검사, 프로덕션 빌드, `git diff --check` 통과.

사용자 작업: 없음. T19에서는 Supabase SQL을 실행하지 않는다. T21 시작 시 `07_session_functions.sql`도 적용 대상에 포함한다.

### T20 — 순위·관리자·사용성 잔여 개선 — 완료

- 실제 종료 회차 기준 최소 3회 순위 조건을 적용한다.
- CSV 수식 인젝션·페이지네이션·KST 표시를 수정하고, matched·SHORT 조회의 안정적인 정렬을 보장한다.
- 홈 예정 회차 분리, 가입 오류 필드 표시, 관리자 조회 실패·로그아웃, SHORT Enter, ORDER 복습, 대시보드 기본 회차를 보완한다.
- 관리자 시각은 KST로 통일하고, 초기 네트워크 실패가 무한 로딩으로 남지 않도록 재시도 상태를 추가한다.

완료 조건: 관리자 대용량 조회와 참가자 경계 화면 회귀검사가 통과한다.

검증 결과: `validate-t20.mjs`, `lib/admin.test.ts` 3개, 기존 grading 30개, 문항은행·사전학습·성과·세션 검증, 타입검사, 프로덕션 빌드, `git diff --check` 통과.

사용자 작업: 없음. 로컬 코드·문서만 변경했으며 Supabase SQL은 실행하지 않았다. T21에서 백업과 행 수 스냅샷을 확인한 뒤 별도 승인을 받아 마이그레이션한다.

### T21 — Supabase 마이그레이션

사용자 명시 승인이 필요한 카드다.

- 먼저 백업·행 수 스냅샷을 만든다.
- 응시 데이터가 없을 때만 6회차 시드와 뷰·함수를 적용한다.
- `db/07_session_functions.sql`의 세션 생성·재조정·만료 RPC도 같은 승인된 마이그레이션 범위에서 적용한다.
- 응시 데이터가 있으면 삭제·초기화하지 않고 사용자 결정까지 중단한다.
- 적용 후 `6회차·72문항·회차당 12문항·측정 12+12·앵커 회차당 2개·본문 6건`을 확인한다.

### T22 — Vercel·Supabase 운영 설정

사용자 Dashboard 접근이 필요한 카드다.

- Vercel Function 리전 `icn1`, Cron 및 `maxDuration`을 운영 플랜에 맞게 설정한다.
- 순위 갱신은 매분, 세션 만료는 5분 주기로 설정한다.
- Vercel 환경변수는 서버 전용으로 등록하고, 관리자 초기 시딩 후 `ADMIN_INIT_PASSWORD`를 제거한다.
- 매분 Cron은 Vercel Pro를 전제로 한다. Hobby는 하루 1회 제한이 있으므로 플랜이 다르면 개시 전에 운영 방식을 결정한다.

#### T21·T22에서 사용자가 할 일 — 초보자용 예고

1. **왜 필요한가:** T21은 실제 DB에 6회차 시드·뷰·사전학습을 반영하는 작업이고, T22는 서비스 중단·Cron 누락·관리자 초기 비밀번호 잔존 위험을 줄이는 운영 설정이다. 이 작업 전까지 로컬 파일만 바뀌며 실DB는 비어 있는 상태를 유지한다.
2. **Supabase 경로:** Supabase Dashboard → 해당 프로젝트 → **Settings → Billing**에서 운영 플랜과 자동 백업·일시정지 조건을 확인한다. 이어 **SQL Editor**에서 T21이 제공하는 백업·행 수 확인 SQL을 먼저 실행한다.
3. **Vercel 경로:** Vercel Dashboard → 프로젝트 → **Settings → Environment Variables**에서 서버 전용 변수를 등록하고, **Settings → Functions**에서 리전을 `icn1`로 확인한다. `vercel.json`의 Cron·함수 시간 설정은 T22에서 파일과 화면을 함께 맞춘다.
4. **입력할 값:** `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SESSION_SECRET`, `CRON_SECRET`, `ADMIN_LOGIN_ID`, `ADMIN_INIT_PASSWORD`는 각각 이름만 확인하고 비밀값은 로컬 파일·Vercel 입력란에 직접 붙여넣는다. `ADMIN_INIT_PASSWORD`는 최초 관리자 시딩 성공 후 삭제한다.
5. **보안 주의:** service role 키·세션 비밀값·초기 관리자 비밀번호를 채팅, Git, 스크린샷, 이메일에 보내지 않는다.
6. **정상 결과:** T21 검증 쿼리에서 `quiz_round=6`, `quiz_item=72`, 회차당 문항 `12`, 측정 `12+12`, 앵커 회차당 `2`, 본문 `6`이 확인되고, T22에서는 Cron 최근 실행 성공·관리자 로그인·`/api/health` 정상 응답을 확인한다.
7. **실패 시 되돌리기:** T21은 사용자 승인 전 실행하지 않으며, 실패하면 추가 SQL을 실행하지 않고 오류 화면·실행한 SQL·행 수만 보존한다. T22는 잘못 입력한 환경변수를 수정·삭제하고 Cron을 수동 재실행한다.
8. **회신 문구:** 해당 카드가 시작되면 안내한 작업을 끝낸 뒤 `T21 백업·행 수 확인 완료` 또는 `T22 운영 설정 완료`라고 회신한다. 비밀값 자체는 회신하지 않는다.

### T23 — 통합·파일럿·부하 테스트와 개시

- 타입검사, 기존 29개 테스트, 문항은행 검사, 빌드를 실행한다.
- 실제 DB에서 가입부터 관리자 CSV까지 전체 흐름을 확인한다.
- 답변 전 정답·해설 미전송, 측정 필드 미전송, 서버 타이머, 재응시 차단을 네트워크 응답으로 검증한다.
- 별도 테스트 환경에서 100 VU 부하 테스트를 수행하고 오류율 0%, p95 500ms 미만을 확인한다.
- 5~10명 파일럿 평균 정답률 70~75%를 확인한다.

개시 차단 조건: 2026-08-16 18:00 KST까지 필수 검증이 끝나지 않으면 `is_published=false`를 유지하고 일정을 재조정한다.

### T24 — 운영 및 종료

- 매주 금요일 CSV 백업, Cron·오류 로그·참여율을 점검한다.
- 포상 인원·금액은 별도 운영 결정으로 기록한다.
- 종료 후 최종 CSV와 성과 요약을 회수하고 보존 기간 승인 뒤 서비스 폐기를 진행한다.

## API·데이터 계약 변경 요약

- 참가자 회차 API의 유효 범위는 `1..6`이다.
- `GET /api/admin/stats/matched`는 12쌍 완전대응 참가자만 반환한다.
- `GET /api/admin/stats/outcomes`를 추가한다.
  - `summary`: 대응 표본 수, 사전·사후 평균, 평균·중앙 향상폭, 향상자 비율
  - `pairs`: M01~M12별 사전·사후 정답률과 향상폭
  - `anchors`: A1·A2 회차별 정답률
  - `transfers`: T01~T04 정답률
- 신규 개인정보, 브라우저 저장소, 클라이언트 Supabase 접근은 추가하지 않는다.

## 카드 완료 보고 형식

각 카드 완료 보고에는 다음 항목만 기록한다.

1. 완료한 카드와 변경 내용
2. 검증 명령과 결과
3. 변경 파일과 커밋
4. 남은 위험 또는 사용자 작업
5. 다음 카드와 사용자가 할 일
